代码说明：
	1.通过data接口来输入数据，clk来切换状态，s1时将数据赋给A，s2时将数据赋给B，s3时将后四位赋给控制码，进行运算并输出运算结果，s0时输出标志位结果。
	2.运算结果通过16个LED灯输出，标志位用七位数码管输出.
	3.标志位有四位，分别标记最高位、是否溢出、是否进位、是否为零的状态。运算的控制码有	四位，可表示16种运算，但是实验要求了10种运算，均通过内置函数实现。
实验结果：
	实现了要求的ALU功能